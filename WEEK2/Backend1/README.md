### steps to ceate backend

1.Generate package.json file
     npm init -y

2.create HTTP Server
   -Install & Import express module
        npm install express (all modules will be installed )
    -Import express module

3.create REST APIs
    resource based operations

    http://localhost:3000/users --->REST End point

4.add middleware
